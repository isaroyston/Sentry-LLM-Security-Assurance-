# Backend 
- prompt_attacks.py -single line prompt attacks from csv files 
- modules folder contain the type of prompt attack modules to edit prompts e.g ( homoglyph.py and payload_mask.py)
- generative_red_team.py reads from the red_team_scenarios.json with suggested prompts and target refusal (how can we make this customisable for users easily)
- change target refusal to a suggestion rather than exact match 
- migrate the evaulator from check_for_success in generative_red_team to evaluator.py and evaluate_model_safety to evaluator.py (simple parser for single prompts)
- red_teaming.py (NEW) should orchestrate and direct all the processes depending on user selection (asynchronous request handling and back off for ratelimits)
- clone .env in attacks so I can package it as another App
- start script with find_env and load_env

# App 

## Fucntionalities
- Connect Chatbot API 
User can select prompt attack methods 
- General (All generic prompt attacks) ; static datasets
- Custom Prompts Dataset - can edit tabular data and 
- Red Team LLM w/ Scenarios (can edit objective and suggested prompt reference)

## Report 

- Show Attack Success Rate Overall
- Show Attack Success Count and Rate, Segment by Prompt Categories
- Show Vulnerabilities Defended against
- Drop down to show specific leakage categories